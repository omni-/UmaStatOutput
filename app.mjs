export const STAT_NAMES = ["Speed", "Stamina", "Power", "Guts", "Wit", "SP"];
export const TYPE_NAMES = ["Speed", "Stamina", "Power", "Guts", "Wit"];
export const RARITY_NAMES = {1: "R", 2: "SR", 3: "SSR"};
export const TRAINING_PROFILES = {
  "gl-late": {label: "Grand Live · late-run", gains: [[11,0,5,0,0,2],[0,9,0,6,0,2],[0,4,10,0,0,2],[3,0,2,10,0,2],[3,0,0,0,9,3]]},
  "gl-summer": {label: "Grand Live · summer", gains: [[12,0,6,0,0,2],[0,12,0,8,0,2],[0,6,13,0,0,2],[3,0,3,11,0,2],[4,0,0,0,10,3]]},
};
const BASE_SPECIALTY_WEIGHT=100,OFF_TRAINING_WEIGHT=100,NO_TRAINING_WEIGHT=50,SUPPORT_COUNT_BONUS=.05;
export function calculateAppearance(card,globalSpecialty=0){const specialtyWeight=(BASE_SPECIALTY_WEIGHT+Number(card.specialty_rate||0)+Number(globalSpecialty||0))*Number(card.unique_specialty||1)*Number(card.fs_specialty||1),denominator=specialtyWeight+OFF_TRAINING_WEIGHT*4+NO_TRAINING_WEIGHT;return{specialtyWeight,denominator,specialty:specialtyWeight/denominator,eachOff:OFF_TRAINING_WEIGHT/denominator,none:NO_TRAINING_WEIGHT/denominator}}
export function calculateMarginalTraining(card,trainingType,options={}){const gains=options.gains||TRAINING_PROFILES["gl-late"].gains[trainingType],motivation=Number(options.motivation??.2),growth=options.growth||[1,1,1,1,1,1],rainbow=Boolean(options.rainbow);let trainingBonus=Number(card.tb||1),motivationBonus=Number(card.mb||1),friendshipBonus=1;if(rainbow){trainingBonus+=Number(card.fs_training||0);motivationBonus+=Number(card.fs_motivation||0);friendshipBonus=Number(card.fs_bonus||1)*Number(card.unique_fs_bonus||1)}const result=new Array(6).fill(0);for(let stat=0;stat<6;stat++){if(!gains[stat])continue;let base=Number(gains[stat])+Number(card.stat_bonus?.[stat]||0);if(rainbow)base+=Number(card.fs_stats?.[stat]||0);const withCard=base*trainingBonus*(1+motivation*motivationBonus)*friendshipBonus*(1+SUPPORT_COUNT_BONUS)*Number(growth[stat]||1);result[stat]=withCard-Number(gains[stat])}return result}
export function weightedSum(vector,spWeight=1.2){return vector.reduce((sum,value,index)=>sum+value*(index===5?spWeight:1),0)}
export function calculateCardEV(card,options={}){const profile=TRAINING_PROFILES[options.profile||"gl-late"]||TRAINING_PROFILES["gl-late"],globalSpecialty=Number(options.globalSpecialty||0),motivation=Number(options.motivation??.2),growth=options.growth||[1,1,1,1,1,1],spWeight=Number(options.spWeight??1.2),appearance=calculateAppearance(card,globalSpecialty),rainbowMarginal=calculateMarginalTraining(card,card.type,{gains:profile.gains[card.type],motivation,growth,rainbow:true}),specialtyVector=rainbowMarginal.map(value=>value*appearance.specialty),offVector=new Array(6).fill(0);for(let trainingType=0;trainingType<5;trainingType++){if(trainingType===card.type)continue;const marginal=calculateMarginalTraining(card,trainingType,{gains:profile.gains[trainingType],motivation,growth,rainbow:false});for(let stat=0;stat<6;stat++)offVector[stat]+=marginal[stat]*appearance.eachOff}const allPlacementVector=specialtyVector.map((value,index)=>value+offVector[index]);return{appearance,rainbowMarginal,specialtyVector,offVector,allPlacementVector,rainbowScore:weightedSum(rainbowMarginal,spWeight),specialtyScore:weightedSum(specialtyVector,spWeight),allPlacementScore:weightedSum(allPlacementVector,spWeight)}}
function conditionalFlags(card){const flags=[];if((card.fs_ramp?.[0]||0)!==0)flags.push("ramping friendship unique");if((card.crowd_bonus||0)!==0)flags.push("crowd-size training unique");if((card.highlander_training||0)!==0)flags.push("deck-diversity training unique");if((card.fan_bonus||0)!==0)flags.push("fan-count training unique");return flags}
function imageUrl(id){return`https://raw.githubusercontent.com/Euophrys/umamusume-tierlist/main/public/cardImages/support_card_s_${id}.png`}
function fmt(value,digits=2){return Number(value).toFixed(digits)}function pct(value){return`${(value*100).toFixed(2)}%`}function rarity(card){return RARITY_NAMES[card.rarity]||`R${card.rarity}`}function lbLabel(lb){return Number(lb)===4?"MLB":`LB${lb}`}function cardTitle(card){return card.title?`[${card.title}]`:""}function htmlEscape(value){return String(value).replaceAll("&","&amp;").replaceAll("<","&lt;").replaceAll(">","&gt;").replaceAll('"',"&quot;").replaceAll("'","&#039;")}
function titleMarkup(card,className="card-titleline"){return card.title?`<div class="${className}">${htmlEscape(cardTitle(card))}</div>`:""}function rarityMarkup(card){return`<span class="rarity-chip">${htmlEscape(rarity(card))}</span>`}function cardSearchText(card){return`${card.char_name} ${card.title||""} ${card.id} ${TYPE_NAMES[card.type]} ${rarity(card)}`.toLowerCase()}
function initBrowser(){const els={sourcePill:document.querySelector("#source-pill"),globalSpec:document.querySelector("#global-spec"),globalSpecRange:document.querySelector("#global-spec-range"),trainingProfile:document.querySelector("#training-profile"),motivation:document.querySelector("#motivation"),spWeight:document.querySelector("#sp-weight"),search:document.querySelector("#card-search"),searchResults:document.querySelector("#search-results"),selectedCards:document.querySelector("#selected-cards"),selectedCount:document.querySelector("#selected-count"),results:document.querySelector("#results"),resultsEmpty:document.querySelector("#results-empty"),resultsBody:document.querySelector("#results-body"),resultDetails:document.querySelector("#result-details"),growthGrid:document.querySelector("#growth-grid"),resetSettings:document.querySelector("#reset-settings")},state={payload:null,groups:[],selected:[],activeDetailId:null};els.search.placeholder="Search title, character, or support ID…";const growthNames=["Speed","Stamina","Power","Guts","Wit"];els.growthGrid.innerHTML=growthNames.map((name,i)=>`<div class="growth-field"><label for="growth-${i}">${name} %</label><input id="growth-${i}" data-growth-index="${i}" type="number" min="0" max="100" step="1" value="0" /></div>`).join("");
function settings(){const growth=[0,1,2,3,4].map(i=>1+Number(document.querySelector(`#growth-${i}`).value||0)/100);growth.push(1);return{globalSpecialty:Number(els.globalSpec.value||0),profile:els.trainingProfile.value,motivation:Number(els.motivation.value),spWeight:Number(els.spWeight.value||1.2),growth}}
function buildGroups(cards){const map=new Map;for(const card of cards){if(!map.has(card.id))map.set(card.id,{id:card.id,lbs:new Map,sample:card});map.get(card.id).lbs.set(card.limit_break,card)}return[...map.values()].sort((a,b)=>b.id-a.id)}function currentCard(sel){const group=state.groups.find(g=>g.id===sel.id);return group?.lbs.get(sel.lb)||group?.lbs.get(Math.max(...group.lbs.keys()))}
function renderSearch(){const query=els.search.value.trim().toLowerCase();if(!query||!state.payload){els.searchResults.hidden=true;return}const already=new Set(state.selected.map(s=>s.id)),matches=state.groups.filter(group=>!already.has(group.id)&&cardSearchText(group.sample).includes(query)).slice(0,20);if(!matches.length){els.searchResults.innerHTML='<div class="search-meta" style="padding:12px">No matching support cards.</div>';els.searchResults.hidden=false;return}els.searchResults.innerHTML=matches.map(group=>{const card=group.lbs.get(Math.max(...group.lbs.keys()))||group.sample,maxLb=Math.max(...group.lbs.keys());return`<button class="search-result" type="button" data-add-id="${card.id}" data-card-type="${card.type}"><div class="accent-card-thumb"><img class="card-thumb" src="${imageUrl(card.id)}" alt="" loading="lazy" /></div><div>${titleMarkup(card,"search-titleline")}<div class="name-row"><div class="search-name">${htmlEscape(card.char_name)}</div>${rarityMarkup(card)}</div><div class="search-meta">${TYPE_NAMES[card.type]} · #${card.id} · LB0–${lbLabel(maxLb)}</div></div><div class="add-mark">＋</div></button>`}).join("");els.searchResults.hidden=false}
function addCard(id){if(state.selected.length>=10||state.selected.some(s=>s.id===id))return;const group=state.groups.find(g=>g.id===id);if(!group)return;state.selected.push({id,lb:Math.max(...group.lbs.keys())});els.search.value="";els.searchResults.hidden=true;renderAll()}function removeCard(id){state.selected=state.selected.filter(s=>s.id!==id);if(state.activeDetailId===id)state.activeDetailId=null;renderAll()}
function renderSelected(){els.selectedCount.textContent=state.selected.length;if(!state.selected.length){els.selectedCards.className="selected-cards empty-state";els.selectedCards.textContent="Search for a card above. Cards are loaded from Euophrys' Global support dataset and default to their highest available limit break.";return}els.selectedCards.className="selected-cards";els.selectedCards.innerHTML=state.selected.map(sel=>{const group=state.groups.find(g=>g.id===sel.id),card=currentCard(sel),lbOptions=[...group.lbs.keys()].sort((a,b)=>a-b).map(lb=>`<option value="${lb}" ${lb===sel.lb?"selected":""}>${lbLabel(lb)}</option>`).join("");return`<div class="selected-card" data-card-type="${card.type}"><div class="accent-card-thumb"><img class="card-thumb" src="${imageUrl(card.id)}" alt="" /></div><div style="min-width:0">${titleMarkup(card)}<div class="name-row"><div class="selected-title" title="${htmlEscape(card.char_name)}">${htmlEscape(card.char_name)}</div>${rarityMarkup(card)}</div><div class="selected-meta"><span class="type-tag">${TYPE_NAMES[card.type]}</span><select data-lb-id="${card.id}">${lbOptions}</select></div></div><button class="remove-card" data-remove-id="${card.id}" type="button" title="Remove">×</button></div>`}).join("")}
function evaluatedCards(){const opts=settings(),rows=state.selected.map(sel=>{const card=currentCard(sel);return{card,ev:calculateCardEV(card,opts),flags:conditionalFlags(card)}});rows.sort((a,b)=>b.ev.specialtyScore-a.ev.specialtyScore);return rows}
function renderDetails(row){if(!row){els.resultDetails.innerHTML="";return}const{card,ev,flags}=row,warning=flags.length?`<div class="warning-box"><strong>Conditional unique:</strong> ${htmlEscape(flags.join(", "))}. The isolated benchmark does not guess the trigger state for these dynamic modifiers, so treat this card's result as a baseline.</div>`:"";els.resultDetails.innerHTML=`<div class="detail-card" data-card-type="${card.type}"><div class="detail-top"><div>${titleMarkup(card)}<div class="name-row"><div class="detail-title">${htmlEscape(card.char_name)}</div>${rarityMarkup(card)}</div><div class="search-meta">${TYPE_NAMES[card.type]} · ${lbLabel(card.limit_break)} · #${card.id} · Specialty Priority ${fmt(card.specialty_rate,0)} · Specialty Rate ${pct(ev.appearance.specialty)}</div></div><div class="formula-chip">#${card.id}</div></div><div class="detail-ev-heading"><strong>Per-turn Specialty EV by stat</strong><span>Appearance-weighted average contribution across all turns.</span></div><div class="stat-vector">${STAT_NAMES.map((name,i)=>`<div class="stat-cell"><span>${name}</span><strong>${fmt(ev.specialtyVector[i])}</strong></div>`).join("")}</div>${warning}</div>`}
function renderResults(){if(!state.selected.length){els.results.hidden=true;els.resultsEmpty.hidden=false;els.resultDetails.innerHTML="";return}els.results.hidden=false;els.resultsEmpty.hidden=true;const rows=evaluatedCards();if(!state.activeDetailId||!rows.some(r=>r.card.id===state.activeDetailId))state.activeDetailId=rows[0]?.card.id;els.resultsBody.innerHTML=rows.map((row,index)=>{const{card,ev,flags}=row;return`<tr data-detail-id="${card.id}" class="${state.activeDetailId===card.id?"active":""}" data-card-type="${card.type}"><td class="rank">${index+1}</td><td><div class="result-support" data-card-type="${card.type}"><div class="accent-card-thumb small"><img class="card-thumb" src="${imageUrl(card.id)}" alt="" /></div><div>${titleMarkup(card,"result-titleline")}<div class="name-row"><div class="result-name">${htmlEscape(card.char_name)}${flags.length?'<span class="warn-dot" title="Context-dependent unique">◆</span>':''}</div>${rarityMarkup(card)}</div><div class="result-sub">${TYPE_NAMES[card.type]} · ${lbLabel(card.limit_break)} · #${card.id}</div></div></div></td><td><div class="metric-main">${fmt(card.specialty_rate,0)}</div><div class="metric-sub">得意率</div></td><td><div class="metric-main">${pct(ev.appearance.specialty)}</div><div class="metric-sub">preferred training appearance</div></td><td><div class="metric-main">+${fmt(ev.rainbowScore)}</div><div class="metric-sub">extra weighted stats</div></td><td class="${index===0?"best":""}"><div class="metric-main">${fmt(ev.specialtyScore)}</div><div class="metric-sub">weighted stats / turn</div></td></tr>`}).join("");renderDetails(rows.find(r=>r.card.id===state.activeDetailId)||rows[0])}
function renderAll(){renderSelected();renderResults()}els.search.addEventListener("input",renderSearch);els.search.addEventListener("focus",renderSearch);document.addEventListener("click",event=>{const add=event.target.closest("[data-add-id]");if(add)addCard(Number(add.dataset.addId));const remove=event.target.closest("[data-remove-id]");if(remove)removeCard(Number(remove.dataset.removeId));const detail=event.target.closest("[data-detail-id]");if(detail){state.activeDetailId=Number(detail.dataset.detailId);renderResults()}if(!event.target.closest(".search-wrap"))els.searchResults.hidden=true});els.selectedCards.addEventListener("change",event=>{const select=event.target.closest("[data-lb-id]");if(!select)return;const item=state.selected.find(s=>s.id===Number(select.dataset.lbId));if(item)item.lb=Number(select.value);renderAll()});[els.trainingProfile,els.motivation,els.spWeight].forEach(el=>el.addEventListener("input",renderResults));document.querySelectorAll("[data-growth-index]").forEach(el=>el.addEventListener("input",renderResults));
function setGlobalSpec(value){els.globalSpec.value=value;els.globalSpecRange.value=Math.max(0,Math.min(100,Number(value)));document.querySelectorAll("[data-spec-preset]").forEach(button=>button.classList.toggle("active",Number(button.dataset.specPreset)===Number(value)));renderResults()}els.globalSpec.addEventListener("input",()=>setGlobalSpec(els.globalSpec.value));els.globalSpecRange.addEventListener("input",()=>setGlobalSpec(els.globalSpecRange.value));document.querySelectorAll("[data-spec-preset]").forEach(button=>button.addEventListener("click",()=>setGlobalSpec(button.dataset.specPreset)));els.resetSettings.addEventListener("click",()=>{setGlobalSpec(0);els.trainingProfile.value="gl-late";els.motivation.value="0.2";els.spWeight.value="1.2";document.querySelectorAll("[data-growth-index]").forEach(el=>{el.value=0});renderResults()});
fetch("./data/cards.json",{cache:"no-cache"}).then(response=>{if(!response.ok)throw new Error(`HTTP ${response.status}`);return response.json()}).then(payload=>{state.payload=payload;state.groups=buildGroups(payload.cards);const date=payload.generated_at?new Date(payload.generated_at).toLocaleDateString():"current build";els.sourcePill.textContent=`${state.groups.length} supports · synced ${date}`;els.sourcePill.classList.add("ready");renderAll()}).catch(error=>{console.error(error);els.sourcePill.textContent="Support data failed to load";els.sourcePill.style.color="var(--danger)";els.selectedCards.className="selected-cards empty-state";els.selectedCards.textContent="The generated card dataset is missing. Run the sync script or GitHub Pages workflow."})}
if(typeof document!=="undefined")initBrowser();
